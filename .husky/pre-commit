#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Running contract tests before commit..."

npm run test:contracts

if [ $? -eq 0 ]; then
  echo "✅ Contract tests passed - safe to commit!"
else
  echo "❌ Contract tests failed! Please fix before committing."
  echo "💡 Run 'npm run test:contracts' to see failures"
  exit 1
fi
